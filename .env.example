# Copy to .env if you need to override the defaults in docker-compose.yml.

# Database password for the containerised PostgreSQL. The compose file has a
# dev-only default so `docker compose up` works with no setup. Set a real one
# for anything shared.
# POSTGRES_PASSWORD=change-me

# Only needed if your checkout uses a different frontend folder name.
# FRONTEND_DIR=Frontend
